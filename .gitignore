# Rust 编译产物(workspace 共享的 target 在根目录)
/target

# Node 依赖(各前端目录的 .gitignore 也会再盖一层)
node_modules

# 说明:
# - app/ 现在是项目正式组成部分,必须纳入版本控制(不再忽略)
# - Cargo.lock 对应用类项目应当提交,锁定依赖版本保证可复现构建

# macOS 目录元数据垃圾文件
.DS_Store

# JetBrains IDE 项目配置
.idea/

# Brainstorm 可视化伴侣临时文件
.superpowers/
